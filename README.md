# WhatsApp Contract Expiration Notifier

Автоматическая система уведомлений клиентов об истечении контрактов через WhatsApp Business API.

## 📋 Возможности

- ✅ Автоматическая отправка уведомлений клиентам за 30 дней до истечения контракта
- ✅ Загрузка данных напрямую из Google Sheets
- ✅ Фильтрация по дате истечения контракта
- ✅ Уведомления администратору об успешной отправке
- ✅ Уведомления администратору об ошибках и превышении лимитов
- ✅ Поддержка запуска через GitHub Actions (бесплатно) или на собственном сервере

## 🚀 Быстрый старт (GitHub Actions)

### 1. Настройка репозитория

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
git push -u origin main
```

### 2. Настройка секретов

Перейдите в **Settings** → **Secrets and variables** → **Actions** и добавьте:

- `ACCESS_TOKEN` - Ваш токен WhatsApp Business API
- `PHONE_NUMBER_ID` - ID вашего номера телефона (например: `957011204161290`)

### 3. Готово!

Бот автоматически запустится каждый день в **09:00 UTC+4**.

Вы также можете запустить его вручную:
- Перейдите во вкладку **Actions**
- Выберите **Daily WhatsApp Sender**
- Нажмите **Run workflow**

## 📁 Структура проекта

```
.
├── .github/
│   └── workflows/
│       └── daily_whatsapp.yml    # GitHub Actions workflow
├── daily_contract_notifier.py    # Основная логика (с расписанием для сервера)
├── run_job.py                    # Точка входа для GitHub Actions
├── requirements.txt              # Зависимости Python
├── DEPLOY_README.md             # Инструкции для деплоя на VPS
└── README.md                    # Этот файл
```

## 🔧 Настройка шаблонов WhatsApp

В Meta Business Manager создайте следующие шаблоны:

### 1. `contract_ru` (для клиентов)
Текст с 4 переменными:
```
Уважаемый {{1}}! Напоминаем, что ваш контракт №{{3}} ({{4}}) истекает {{2}}. Пожалуйста, свяжитесь с нами для продления.
```

### 2. `messages_sent` (для администратора)
Текст с 1 переменной:
```
Уведомления об истечении контрактов отправлены: {{1}}
```

### 3. `admin_alert` (для ошибок)
Текст с 1 переменной:
```
Внимание! Ошибки рассылки: {{1}}
```

## 📊 Формат Google Sheets

Таблица должна содержать следующие колонки:

| Имя | ООО | Номер телефона | Номер контракта | Описание контракта | Дата истечения |
|-----|-----|----------------|-----------------|-------------------|----------------|
| Иван | ООО Компания | 79991234567 | 123 | Услуги | 15.02.2026 |

**Важно:** Формат даты должен быть `ДД.ММ.ГГГГ`

## 🖥️ Запуск на собственном сервере

См. подробные инструкции в [DEPLOY_README.md](DEPLOY_README.md)

Краткая версия:
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python3 daily_contract_notifier.py
```

## ⚙️ Конфигурация

Основные параметры в `daily_contract_notifier.py`:

- `SHEET_ID` - ID вашей Google таблицы
- `TEMPLATE_NAME` - Имя шаблона для клиентов
- `ADMIN_TEMPLATE_NAME` - Имя шаблона для успешных уведомлений
- `ADMIN_ERROR_TEMPLATE` - Имя шаблона для ошибок
- `ADMIN_PHONE_NUMBER` - Номер телефона администратора

## 🔒 Безопасность

- Токены хранятся в GitHub Secrets (не в коде)
- Используется `os.getenv()` для чтения конфиденциальных данных
- Токен в коде используется только как fallback для локального запуска

## 📝 Логика фильтрации

Уведомления отправляются только если:
- Дата истечения контракта ≤ Сегодня + 30 дней
- Номер телефона указан в таблице
- Дата корректно распознана

## 🐛 Отладка

Проверьте логи:
- **GitHub Actions**: вкладка Actions → выберите запуск → просмотрите логи
- **Локально**: вывод в консоль

Типичные ошибки:
- `131030` - Номер не в списке разрешенных (добавьте в Meta Dashboard)
- `131048` - Превышен лимит / спам-фильтр
- `Invalid Date` - Неверный формат даты в таблице

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Корректность токена и Phone Number ID
2. Статус шаблонов в Meta Business Manager (должны быть Approved)
3. Формат данных в Google Sheets
4. Настройки GitHub Secrets
